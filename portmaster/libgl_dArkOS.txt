#!/bin/bash
#
# SPDX-License-Identifier: MIT
# Optimized GL4ES configuration for dArkOS

source "${controlfolder}/libgl_default.txt"

export LIBGL_NOERROR=1
export LIBGL_NOBANNER=1

if [ -f "$HOME/.config/portmaster_show_fps" ]; then
  export LIBGL_NOBANNER=0
  export LIBGL_FPS=1
fi
